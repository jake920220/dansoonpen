# 한국어·영어 지원 준비 검증 (2026-09-21)

## 기준과 적용 상태

- 구현 기준: main의 `4b00142`(v0.5.1). 이 작업의 v0.6.0 빌드는 언어 기능 검사용 후보이며 공개 릴리스가 아니다.
- 작업 도중 실제 실행 앱이 v0.6.3이고, `codex/all-displays-drawing` 별도 작업 폴더에 해당 기능의 미커밋 변경이 있음을 확인했다.
- 기존 v0.6.3 앱 번들을 보존·복원했다. 언어 후보를 현재 사용 앱에 적용하거나 최신 다중 화면 변경과 통합하지 않았다. 통합 기준에 대한 사용자 답변 대기 중이다.
- 현재 README는 이 소스 트리의 동작을 설명한다. 최신 작업본을 통합하면 다중 화면·공유 도구막대 설명도 함께 갱신해야 한다.

## 구현

- 설정의 한국어/English 선택, 설정 파일의 언어 보존과 이전 스키마 마이그레이션.
- 설정창·도구막대·툴팁·접근성 이름·상태 안내·트레이 메뉴의 번역. 웹 UI와 Rust 메뉴가 같은 영문 카탈로그를 사용한다.
- 필기 내용·사용자 프리셋 이름·오류의 파일 경로와 OS 세부 내용은 보존한다.
- 외부 번역 서비스나 새 런타임 의존성을 추가하지 않았다.

## 수행한 검증

| 항목 | 결과와 한계 |
| --- | --- |
| Svelte/TypeScript 검사 | 오류 0, 경고 0 |
| 프런트엔드 테스트 | 12개 파일, 56개 통과. 언어 전환, 보간 값 보존, 카탈로그 누락·치환자 검사 포함 |
| Rust 테스트 | 46개 통과. 이전 설정 마이그레이션, 저장·다시 읽기, 다른 설정 변경 시 언어 유지, 메뉴 번역 포함 |
| Rust 형식·Clippy | 통과, 경고를 오류로 처리 |
| 의존성 고지 | 303개 의존성과 포함 폰트 검사 통과 |
| macOS arm64 후보 빌드 | 성공, 로컬 ad-hoc 서명 검사 통과. 공증·다른 Mac 설치 검증 아님 |
| 브라우저 UI | 설정에서 English 선택 후 영문 표시와 기존 한글 프리셋 이름 유지 확인. 설정창 상단 화면 배치 확인 |
| 실제 앱 | 기존 v0.6.3 실행 상태와 버전 확인만 수행. 언어 후보의 네이티브 전환·트레이·재시작 유지 검증은 미수행 |

브라우저 검사는 네이티브 창 입력, OS 포커스, 전역 단축키, 화면공유 출력을 검증하지 않는다. Windows 실기 검증도 수행하지 않았다.

## 공개 준비의 제한적 점검

Git의 모든 로컬 참조에서 2MB 이하 이력 blob 354개에 대해 대표적인 개인 키·GitHub 토큰·AWS 액세스 키·OpenAI 토큰 패턴을 검사했고 일치 항목은 없었다. 이는 비밀정보가 전혀 없다는 보증이나 전체 보안 감사가 아니다.

개인 사용자 경로가 포함된 이력 파일은 다음과 같으며, 공개 의도를 확인해야 한다. 실제 경로·비밀 값은 이 기록에 복사하지 않았다.

- `docs/testing/stability-2026-09-08.md`
- `docs/testing/text-drag-selection-2026-09-15.md`
- `src/app/diagnostics.test.ts`

원격 저장소 연결, push, 공개 릴리스, 홍보 게시 및 Git 이력 재작성은 수행하지 않았다.

## 통합 후 남은 확인

1. v0.6.3 다중 화면·도구막대·입력 복구 변경을 보존하고 추가 UI 문구를 번역한다.
2. 통합 버전에서 자동 검증을 다시 수행한다.
3. 실제 앱에서 언어 전환, 각 화면 도구막대·트레이 반영, 재시작 후 언어·기존 설정 보존을 확인한다.
4. 공개할 버전·README·설치 파일·체크섬을 같은 기준으로 맞춘다.
