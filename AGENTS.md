# 프로젝트 작업 지침

- 사용자 호칭은 준현님으로 한다.
- 커밋 제목은 `feat(scope): 한글 목적` 등 Conventional Commits 형식으로 간결하게 작성하고, 본문에 세부 작업 목록을 남긴다.
- 스테이징 전에 `git status`와 변경 파일 목록을 확인하고 무엇을 왜 스테이징하는지 한국어 한 줄로 설명한다.
- 작업 관련 파일만 선택하여 스테이징하고 가능한 기능 단위 원자 커밋으로 만든다.
- 커밋 전에 diff를 검토하고 실행한 검증 결과를 짧게 보고한다. 위험하거나 범위가 큰 커밋은 계획을 먼저 제시해 승인받는다.
- 사용자가 명시적으로 요청하지 않으면 리모트 연결, push, 공개 릴리스를 하지 않는다.
- v0.1은 펜·굵기·색·한글 텍스트·지우개·undo/redo·수동 fade·모드 전환·다중 모니터에 한정한다. 도형·화살표·커서 강조는 고도화에서 다룬다.
- 시간 경과나 조작 모드 전환만으로 그림을 삭제하지 않는다. 전체 삭제 중 새로 그린 객체는 보존한다.
- 화면 오버레이의 창 입력·OS 포커스 검증을 브라우저 테스트로 대체하지 않는다. 미검증 Windows/화면공유 조합을 지원 확인으로 표시하지 않는다.
